# Private Evaluator Material

`answer_key.jsonl` contains evaluator-only answer material matched to the
public repr50 puzzle ids.

This directory must not be copied into agent workspaces or exposed through
task context, prompts, shared references, or any benchmark API that a solving
agent can inspect.
