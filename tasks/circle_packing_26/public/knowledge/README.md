# Circle Packing Knowledge Bundle

This directory contains optional read-only task context for workers. The layout
is task-defined and should be inspected directly from `task/knowledge/` in a
worker workspace.
